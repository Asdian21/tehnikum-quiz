import React from "react";

export const ErrorMessage = (hasError, errorText) => {
  return "";
  // <span id="error-message">
  //   {hasError && <span id="error-message">{errorText}</span>}
  // </span>
};
